import 'package:flutter/material.dart';

class MySubjectsDoctor extends StatelessWidget {
  const MySubjectsDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
